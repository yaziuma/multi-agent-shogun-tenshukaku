# python実行
* `uv`コマンドのみ利用可能。`python`, `python3`コマンドでの直接実行は不可

# コード修正後の起動
* `restart.sh`を使用して起動すること。キャッシュの削除も内包しているので、変更が反映される。

# 禁止事項

* フォールバックはどんな理由があろうと一切禁止。
* 調査・レビュー結果は必ず記録に残す。変更がある場合は必ず変更する。後から見てもセッション切り替え後に見ても他のエージェントが見ても齟齬が発生しないように整合性を保つこと。
* 所要時間を見積もらない。
